<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PBAGreenSpots" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/teal_200</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="bottomNavigation">
        <item name="android:background">@color/green_normal</item>
        <item name="itemIconTint">@color/green__light2</item>
        <item name="android:textColor">@color/green__light2</item>

    </style>
</resources>